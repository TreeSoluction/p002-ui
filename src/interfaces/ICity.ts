export interface ICity {
  id: string;
  nome: string;
  imagem: string;
}

export interface ICityCategory {
  id: string;
  name: string;
  image: string;
}

export interface IStateCardProps {
  name: string;
}
